name: resto
description: "Resto POS - Point of Sale Restaurant App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  cloud_firestore: ^5.6.6
  provider: ^6.1.4
  intl: ^0.20.2
  google_fonts: ^6.2.1
  fl_chart: ^0.70.2
  uuid: ^4.5.1
  image_picker: ^1.1.2
  esc_pos_utils: ^1.1.0
  flutter_blue_plus: ^1.35.2
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
